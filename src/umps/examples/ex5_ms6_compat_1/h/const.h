/* utility constants */
#define	TRUE		1
#define	FALSE		0

#define HIDDEN		static

#define NULL ((void *)0xFFFFFFFF)

#define EOS	'\0'

/* operations */
#define	MIN(A,B)	((A) < (B) ? A : B)
#define MAX(A,B)	((A) < (B) ? B : A)
