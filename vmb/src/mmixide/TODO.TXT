nicer icons, possibly 16x16 bit

implement register stack view together with locals
double click in the X in the splitter bar should close the current document not the edit window.

Tracing MMIX in subwindow not in console window
Step over and step out

Exit with unnamed  window should ask for "save as"

Breakpoints window
Set breakpoints in symbol table
Be able to manage breakpoints even without source code.
Manage read and write breakpoints  
   
Memory view unrestricted

Ask for reload after assembly while debugging

store options in registry to make them persistent.

integrate the generation of image files
	or other postprocessing

Add help for the ide

Add button for build/assemble

Other Ideas:

Add a reset button (doubling that of the motherboard).

make mmixvd (visual debugger for mmix)